#Delete Whole tuple using del keyword

tuple1 = (1, 2, 3, 4, 5)
print("Original tuple:", tuple1)    
del tuple1 #or can write as del(tuple1)
print("After deleting tuple:", tuple1)  # This will raise an error because the tuple has been deleted